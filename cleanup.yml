---
- name: Cleanup Satellite Configuration Drift
  hosts: localhost
  become: false
  gather_facts: false
  vars_files:
    - vars/lab_connection.yml
    - vars/locations.yml
    - vars/mappings/locations.yml
  vars:
    cleanup_apply_changes: false   # dry-run by default; override with -e cleanup_apply_changes=true
    protected_locations:
      - "Default Location"         # Satellite built-in default, never delete
  tasks:
    - name: Cleanup Locations
      ansible.builtin.include_tasks: playbooks/tasks/cleanup/cleanup_locations.yml
      tags: 'locations'
